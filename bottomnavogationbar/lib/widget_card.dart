import 'package:flutter/material.dart';

class MyWidget_card extends StatelessWidget {
  const MyWidget_card({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("this is card screen"),
    );
  }
}
