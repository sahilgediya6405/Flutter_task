import 'package:flutter/material.dart';

class MyWidget_home extends StatelessWidget {
  const MyWidget_home({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("this is home screen"),
    );
  }
}
