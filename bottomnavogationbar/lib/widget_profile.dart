import 'package:flutter/material.dart';

class MyWidget_profile extends StatelessWidget {
  const MyWidget_profile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("this is profile page"),
    );
  }
}
