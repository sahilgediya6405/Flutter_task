import 'package:flutter/material.dart';

class My_Share extends StatelessWidget {
  const My_Share({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: double.infinity,
      width: double.infinity,
      color: Colors.orange,
      child: Center(
        child: Text("Empty"),
      ),
    );
  }
}
